import 'package:flutter/material.dart';


class FaviorateScreen extends StatelessWidget {
  
  const FaviorateScreen({super.key,});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
      body:Center(child: Text("My ELearning App\nFaviorate Screen"),),
    );
  }
}
